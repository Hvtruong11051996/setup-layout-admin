import { Header } from 'antd/es/layout/layout';
import React from 'react';

const HeaderNav = () => {
  return <Header style={{ position: 'sticky', top: 0, zIndex: 1, width: '100%' }} />;
};

export default HeaderNav;
